# frameworkVersion: '1' - '3'

service: show-hooks-test

plugins:
  localPath: serverless/plugins
  modules:
    - serverless-webpack
    - ../src/show-hooks # Load after all plugins for which you want it to show the hooks.

provider:
  name: fn
